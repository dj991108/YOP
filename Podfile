# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'


target 'YOP' do
  use_frameworks!
  pod 'SDWebImage', '~> 5.0'
  pod 'SDWebImageWebPCoder' # WebP 포맷 지원을 위한 추가적인 포드
end
